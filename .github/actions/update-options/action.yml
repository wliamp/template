description: "Updates the init-service workflow to include the new scope in its selectable options."
inputs:
  scope:
    description: "business domain scope"
    required: true
runs:
  using: node20
  main: dist/index.js
